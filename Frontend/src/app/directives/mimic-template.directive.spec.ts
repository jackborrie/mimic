import { MimicTemplateDirective } from './mimic-template.directive';

describe('MimicTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new MimicTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
