import { MDialogDirective } from './m-dialog.directive';

describe('MDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new MDialogDirective();
    expect(directive).toBeTruthy();
  });
});
