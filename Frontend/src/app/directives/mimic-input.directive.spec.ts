import { MimicInputDirective } from './mimic-input.directive';

describe('MimicInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MimicInputDirective();
    expect(directive).toBeTruthy();
  });
});
