import { MimicButton } from './mimic-button.directive';

describe('MimicButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new MimicButton();
    expect(directive).toBeTruthy();
  });
});
