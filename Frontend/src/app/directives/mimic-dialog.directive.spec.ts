import { MimicDialogDirective } from './mimic-dialog.directive';

describe('MimicDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new MimicDialogDirective();
    expect(directive).toBeTruthy();
  });
});
