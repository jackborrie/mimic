<html>

<head>
    <title>Mimic books</title>
    <style>
        html,
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            width: 100vw;
            color: black;
            font-size: 30px;
            display: flex;
            --foreground-0: #002b36;

            overflow-x: hidden;
        }

        .body {
            flex-grow: 1;
            padding: 1rem;
            display: flex;
            flex-direction: row;
            column-gap: 1rem;
        }

        .books {
            max-width: calc(100vw - 24px);
            position: relative;
            overflow-x: hidden;
        }

        .book:nth-child(even) {
            background: lightblue;
        }

        .book {
            display: flex;
            align-items: center;
            overflow-x: hidden;
            white-space: nowrap;
        }

        .book {
            padding-top: 6px;
        }

        a {
            width: 100%;
        }

        img {
            max-width: 150px;
            max-height: 200px;
        }
    </style>
</head>

<body>
<div class="body">
    {{{body}}}
</div>
<script src="js/script.js"></script>
</body>

</html>
